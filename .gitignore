# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*
!.env.example

# Google Search Console — clé de compte de service (accès complet à la
# propriété), jamais commitée. Voir PLAN_INDEXATION_HUMANISATION.md.
*service-account*.json
gsc-coverage-report.json

# Artefact de build interne (liste des pages "offre" pour le contrôle de
# complétude llms.txt) — régénéré à chaque build, voir vite.config.ts.
.offers-manifest.json
